#include <bits/stdc++.h>

using namespace std;

int main()
{
    uint16_t n;
    static array<int64_t, MAX> A, B;
    static array<pair<int64_t, vector<int64_t> >, MAX> R;
    for(uint16_t i = 0; i < n; i++)
        cin >> A[i];
    for(uint16_t i = 0; i < n; i++)
        cin >> B[i];
    for(uint16_t i = 0; i < n; i++)
    {

    }
}
